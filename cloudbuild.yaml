steps:
  - name: gcr.io/cloud-builders/docker
    args:
      - build
      - -t
      - us-central1-docker.pkg.dev/$PROJECT_ID/x-account-monitor/x-account-monitor:latest
      - .

images:
  - us-central1-docker.pkg.dev/$PROJECT_ID/x-account-monitor/x-account-monitor:latest

options:
  logging: CLOUD_LOGGING_ONLY
